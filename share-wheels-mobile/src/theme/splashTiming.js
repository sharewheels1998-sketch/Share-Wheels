/** Minimum time splash stays visible (bootstrap auth check). */
export const MIN_BOOTSTRAP_SPLASH_MS = 4500;

/** Intro splash: delay before Get Started is shown. */
export const INTRO_CTA_DELAY_MS = 2800;

/** Native launch + JS root background (matches splash gradient top). */
export const SPLASH_LAUNCH_BACKGROUND = "#0F172A";
